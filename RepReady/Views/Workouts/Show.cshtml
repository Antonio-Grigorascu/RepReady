@model RepReady.Models.Workout

@if (TempData["message"] != null)
{
    <div class="alert @ViewBag.Alert p-3 rounded-3 text-center mb-5">
        @ViewBag.Message
    </div>
}
<br />

<div class="card">
    <partial name="WorkoutInfo" model="Model"></partial>
    @if ((ViewBag.AfisareButoane == true && Model.OrganizerId == ViewBag.UserCurent) || ViewBag.EsteAdmin == true)
    {
        <div class="d-flex flex-row justify-content-between">
            <a class="btn btn-success" asp-controller="Workouts" asp-action="Edit" asp-route-id="@Model.Id">Editeaza antrenament</a>
            <form method="post" asp-controller="Workouts" asp-action="Delete" asp-route-id="@Model.Id">
                <button class="btn btn-danger" type="submit">Sterge antrenament</button>
            </form>
        </div>
    }
</div>
<br />
<br />

@*
 Afisare exercitii impreuna cu butoanele de editare si stergere 
@foreach (var ex in Model.Exercises)
{
    <div class="container">
        <div class="row">
            <div class="col-md-2"></div>
            <div class="col-md-8">
                <div>
                    <p>@ex.Title</p>
                    <small>@ex.Description</small>
                </div>
                <br>
                @if (ViewBag.Workout.OrganizerId == ViewBag.UserCurent || ViewBag.EsteAdmin == true)
                {
                    <div class="d-flex">
                        <div>
                            <a class="btn btn-outline-primary" asp-controller="Exercises" asp-action="Edit" asp-route-id="@ex.Id">
                                <i class="bi bi-pencil-square"></i> Editeaza
                            </a>
                        </div>
                        <div>
                            <form method="post" asp-controller="Exercises" asp-action="Delete" asp-route-id="@ex.Id">
                                <button class="btn btn-outline-danger" type="submit"><i class="bi bi-trash"></i>Sterge</button>
                            </form>
                        </div>
                    </div>
                }
                <br />
            </div>
            <div class="col-md-2"></div>
        </div>
    </div>
}

 Afisarea formularului in care se poate adauga un exercitiu 
<div class="container">
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            <div class="card">
                <form method="post" asp-controller="Workouts" asp-action="Show">
                    <div class="card-body">
                        <input type="hidden" name="WorkoutId" value="@Model.Id" />
                        <input class="form-control" name="Ttile"/>
                        <label>Descriere exercitiu</label>
                        <br />
                        <textarea class="form-control" name="Description"></textarea>
                        <span asp-validation-for="Description" class="text-danger"></span>
                        <br /><br />
                        <button class="btn btn-success" type="submit">Adauga exercitiul</button>
                    </div>
                </form>
            </div>
        </div>
        <div class="col-md-2"></div>
    </div>
</div>
*@